# Algoritmo_aproximacion---Python
Este algoritmo es similar a la enumeracion exhaustiva, pero no necesita una respuesta exacta. Pode,os aproximar soluciones con un margen de error que llamaremos epsilon
